using JobMiner.Core.Entities;

namespace JobMiner.Matching
{
    /// <summary>
    /// Calculates a relevance score between a vacancy and a candidate profile.
    /// Full implementation is a future step.
 /// </summary>
    public class MatchingEngine
  {
        public int Score(JobVacancy vacancy, CandidateProfile profile)
        {
            // TODO: implement keyword/skills matching logic
         return 0;
   }
    }
}
